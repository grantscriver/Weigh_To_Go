const express = require("express");
const router = express.Router();

/*router.get("/", (req, res) =>
    db.Authentication.findAll()
    .then(Authentication =>{
        console.log(db.Authentication);
    })
   .catch(err => console.log(err)));
*/

//add user
/*router.post("/api/models/Authentication", (req, res) => {
    db.Authentication.create({

    }).then 
})    
*/

module.exports = router;